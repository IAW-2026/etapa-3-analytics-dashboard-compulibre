- [x] Update `src/app/page.tsx` to display both `shipping.completedDeliveries` and `shipping.pendingDeliveries` as KPI cards.
- [x] Verify the UI renders the new KPI card using the existing `GET /api/analytics/summary` fetch.



